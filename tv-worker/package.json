{
  "name": "tv-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that validates TradingView webhooks and forwards them to the local Playwright bridge.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241011.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.80.0"
  }
}
